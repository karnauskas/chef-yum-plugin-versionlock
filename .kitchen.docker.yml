---
driver:
  name: docker

transport:
  name: docker

provisioner:
  name: chef_zero

platforms:
  - name: centos-7
  - name: centos-8

verifier:
  name: inspec

suites:
  - name: default
    run_list:
      - recipe[yum-plugin-versionlock::default]
      - recipe[test::lwrp]
