[main]
enabled = <%= @enabled %>
locklist = <%= @locklist %>
show_hint = <%= @show_hint %>
#  Enable this to lock out "upgrade via. obsoletes" etc. (slower)
follow_obsoletes = <%= @follow_obsoletes %>
